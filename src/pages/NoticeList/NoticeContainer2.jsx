import React from "react";
import styled from "styled-components";
import TopContainer from "./TopContainer";

const NoticeContainer2 = () => {
  return (
    <Wrapper>
      <TopContainer />
    </Wrapper>
  );
};

const Wrapper = styled.div``;

export default NoticeContainer2;
